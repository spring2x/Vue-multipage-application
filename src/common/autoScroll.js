const autoScroll = function(){
  // let auotScrollTimet = null;
  // let $autoScroll = $('#auot-scroll');
  // let scrollHeight = $autoScroll.get(0).scrollHeight;
  // let iHeight = $autoScroll.height();
  // let speed = 2;

  // startAutoScroll();

  // function startAutoScroll() {
  //  auotScrollTimet = setInterval(function() {
  //    let scrollTop = $autoScroll.scrollTop();
  //    if (iHeight + scrollTop >= scrollHeight || scrollTop <= 0) {
  //      speed *= -1;
  //    }
  //    $autoScroll.scrollTop(scrollTop + speed)
  //  }, 30);
  // }

  // $autoScroll.on('mouseenter', function() {
  //  clearInterval(auotScrollTimet)
  // }).on('mouseleave', function() {
  //  startAutoScroll();
  // })
}

export default autoScroll;
